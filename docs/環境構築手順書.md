# ヘルプステーション シフト管理・予約管理システム 環境構築手順書

- 作成日: 2026-09-15
- 前提資料: [技術仕様書](./技術仕様書.md)（1章の技術スタックを前提とする）、[CLAUDE.md](../CLAUDE.md)（開発環境・サーバーに必要な環境の一覧）
- ステータス: OS別（Ubuntu／Debian系・RHEL系）の手順に対応（2026-09-16）

本書は、開発者が自前サーバーにSSHで接続して開発環境を構築する際の、実際の手順をコマンド付きでまとめたものである。サーバーに必要な環境の一覧は CLAUDE.md を参照。

## 前提

- OS：サーバーのOS（ディストリビューション）に応じて、下記手順の「Ubuntu／Debian系」または「RHEL系」のいずれかを参照する。想定OSはUbuntu 22.04 LTS（Ubuntu／Debian系）。
- サーバーは開発者2名（山本・奥野）で共用し、各自が個別のSSHユーザーで接続する。
- サーバーは社内に設置し、Tailscaleに参加済みである。奥野は社外から接続するため、SSH・開発サーバーへのアクセスは（社内LANからの直接アクセスではなく）Tailscale経由を前提とする。ルーターのポートフォワーディングは行わない。

## 手順（OS依存部分：1〜6）

### Ubuntu／Debian系の場合

```bash
# 1. パッケージ更新
sudo apt update && sudo apt upgrade -y

# 2. PHP 8.3系＋拡張機能（Ubuntu標準リポジトリにない場合はppa:ondrej/phpを追加）
sudo add-apt-repository ppa:ondrej/php -y
sudo apt update
sudo apt install -y php8.3 php8.3-cli php8.3-fpm php8.3-mbstring php8.3-xml \
  php8.3-curl php8.3-zip php8.3-mysql php8.3-bcmath php8.3-gd php8.3-intl

# 3. Composer
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer

# 4. Node.js 20系（LTS）
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 5. MySQL 8.0
sudo apt install -y mysql-server
sudo mysql_secure_installation

# 6. Git
sudo apt install -y git
```

### RHEL系（CentOS Stream・Rocky Linux・AlmaLinux等）の場合

```bash
# 1. パッケージ更新
sudo dnf update -y

# 2. PHP 8.3系＋拡張機能（標準リポジトリはバージョンが古いため、EPEL・Remiリポジトリを追加）
sudo dnf install -y https://dl.fedoraproject.org/pub/epel/epel-release-latest-9.noarch.rpm
sudo dnf install -y https://rpms.remirepo.net/enterprise/remi-release-9.rpm
sudo dnf module reset php -y
sudo dnf module enable php:remi-8.3 -y
sudo dnf install -y php php-cli php-fpm php-mbstring php-xml php-curl \
  php-zip php-mysqlnd php-bcmath php-gd php-intl

# 3. Composer
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer

# 4. Node.js 20系（LTS）
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo dnf install -y nodejs

# 5. MySQL 8.0
sudo dnf install -y mysql-server
sudo systemctl enable --now mysqld
sudo mysql_secure_installation

# 6. Git
sudo dnf install -y git
```

## 手順（共通部分：7〜11、OSによらず同じ）

```bash
# 7. 開発者ごとのSSHユーザー作成・公開鍵登録
# Ubuntu／Debian系: sudo adduser <ユーザー名>
# RHEL系          : sudo useradd -m <ユーザー名> && sudo passwd <ユーザー名>
sudo adduser yamamoto   # RHEL系の場合は useradd -m に読み替え
sudo adduser okuno      # RHEL系の場合は useradd -m に読み替え
# 各自 ~/.ssh/authorized_keys に公開鍵を登録

# 8. リポジトリのクローンと依存関係インストール（各開発者が自分のホームディレクトリで）
git clone <repo-url>
cd helpstation
composer install
npm install
cp .env.example .env
php artisan key:generate

# 9. 開発者ごとのDB作成・.env設定・マイグレーション
mysql -u root -p -e "CREATE DATABASE helpstation_dev_yamamoto;"
# .envのDB_DATABASEを自分のDB名に設定してから
php artisan migrate --seed

# 10. ポート開放（tailscale0インターフェース限定。ルーターのポート開放は不要）
# Ubuntu／Debian系（ufw）
sudo ufw allow in on tailscale0 to any port 8001 proto tcp
# RHEL系（firewalld）の場合は以下を使う
# sudo firewall-cmd --permanent --zone=trusted --add-interface=tailscale0
# sudo firewall-cmd --permanent --zone=trusted --add-port=8001/tcp
# sudo firewall-cmd --reload

# 11. 開発サーバー起動（--hostを明示しないとサーバー自身からしかアクセスできない点に注意。ポートは開発者ごとに分ける）
php artisan serve --host=0.0.0.0 --port=8001
npm run dev
```

## アクセス方法

- サーバー上で `tailscale ip -4` を実行し、サーバーのTailscale IPを確認する。
- 各開発者は自分のPCのブラウザから `http://<サーバーのTailscale IP>:<自分のポート番号>` にアクセスする。

| 開発者 | DBスキーマ | ポート |
|---|---|---|
| 山本 | `helpstation_dev_yamamoto` | 8001 |
| 奥野 | `helpstation_dev_okuno` | 8002 |

## 補足

- `.env` は開発者ごとに用意し、Git管理対象外とする（リポジトリ構造定義書1.1参照）。
- 同一サーバー上で2名が同時に開発するため、作業ディレクトリ・DBスキーマ・`php artisan serve`使用時のポート番号は開発者ごとに分け、衝突を避ける（割り当ては上表の通り）。
- ポート開放は`ufw`を`tailscale0`インターフェースに限定することで、Tailscaleに参加していない第三者からのアクセスを防ぐ。
